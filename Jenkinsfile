#! groovy

pipeline {
    agent { label 'build'}
        options { skipDefaultCheckout() }
            stages {
                    stage('1. Collect information') {
		steps {
           	 }
            }
       }
   }
}
